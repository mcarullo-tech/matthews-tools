import math

def combinations(n, r):
    return math.factorial(n) // (math.factorial(r) * math.factorial(n - r))

# Get input from user
n, r = map(int, input("Enter n and r separated by a comma: ").split(','))

print(combinations(n, r))
